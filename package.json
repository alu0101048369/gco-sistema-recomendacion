{
  "name": "gco-sistema-recomendacion",
  "version": "0.1.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "rm -Rf dist && tsc && cp src/html/* dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.2.2"
  }
}
